const myArray = ["camel", 265, true, "5.6"];

const bulb = { watts: 10, type: "LED" };

const multiplyByFour = function (number) {
  //let multiply;
  //if (number.isDigit) {
  let multiply = number * 4;
  //}
  return multiply;
};
export { myArray, bulb, multiplyByFour };
